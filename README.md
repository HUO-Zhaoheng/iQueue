# Queue
dev和dev_front两个分支分别为后端和前端，但之后只在master下提交，不再在分支下提交
以后提交慎用push -f
# Server是后端

运行说明: 

- 在myeclipse中打开项目后，右键 选择`Run as`->`Maven install`看到build successful
- 重复`Run as` 选择`Maven build`,在弹出的对话框里面 **Goals**表单中输入`tomcat7:run` 看到部署成功后给出的本地端口号和uri路径

#iQueue-front-end是前端

直接运行.sln文件即可
